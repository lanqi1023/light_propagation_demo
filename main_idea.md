# 关键共识

## 一句话方案

以互强度作为非相干光传播的理论基础，以随机相位/相干模态作为计算实现，并把第一层统一参数化为

$$
\boxed{
\phi_1=\phi_{\rm lens}+\Delta\phi_1
}
$$

从而在“固定理想透镜模式”和“透镜初始化后端到端训练模式”之间灵活切换。

## 核心模型

1. 多层系统的相干传播算子为

   $$
   \mathbf U_{\boldsymbol\theta}
   =\mathbf P_L\mathbf M_L\cdots\mathbf P_1\mathbf M_1.
   $$

2. 非相干/部分相干光传播互强度：

   $$
   \mathbf J_{\rm out}
   =\mathbf U_{\boldsymbol\theta}
   \mathbf J_{\rm in}
   \mathbf U_{\boldsymbol\theta}^\dagger.
   $$

3. 对互不相干的 MicroLED 输入：

   $$
   \mathbf J_{\rm in}
   =\sum_i x_i\mathbf J_i,
   $$

   因而传感器输出严格满足

   $$
   \boxed{
   \mathbf y=\mathbf A_{\boldsymbol\theta}\mathbf x
   }.
   $$

4. 矩阵元素为输入模式到探测器的功率耦合：

   $$
   A_{ji}
   =\left\|
   \mathbf D_j^{1/2}
   \mathbf U_{\boldsymbol\theta}
   \boldsymbol\psi_i
   \right\|_2^2\ge0.
   $$

## 已确认的关键判断

- 点源逐列传播是完全非相干光的精确模型；随机相位平均是它的无偏随机估计。
- 不显式存储四维互强度，也不在训练中显式构造完整 $8192\times49\,152$ 矩阵。
- 部分相干源优先采用 KL/相干模态分解；严格完全非相干的大规模输入优先采用随机相位或正交编码。
- 多层任意相位超表面通常是空间变系统，不能普遍简化为 OTF 卷积。
- 输出矩阵非负。无损且完整收集时列和为 1；真实有限探测系统通常满足

  $$
  \sum_jA_{ji}\le1.
  $$

- 输入输出维数不同后，通常没有“行和为 1”的要求。
- 任意非负目标矩阵可先按最大列和归一化，多余光导向垃圾端口，电子端恢复全局尺度；正负权重采用双轨/平衡探测。
- 能量约束只是必要条件，有限层相位系统还受 unistochastic 结构、参数数量、NA、孔径和空间带宽积限制。
- “不同 MicroLED 相互不相干”不等于“单颗 MicroLED 内部只有一个相干模式”。最终模型需要考虑单颗 LED 的有限面积、角分布、谱宽和偏振。
- 严格空间非相干源面上的局域纯相位层无效，因此要让每个 LED 在第一层形成覆盖多个相位像素的相干/部分相干扩展模式。

## 第一层和输入光路的共识

- 不优先采用“把 LED 像直接成在第一层”的结构，因为严格非相干像面会削弱第一层纯相位的作用。
- 更适合的输入编码是将 LED 位置映射为入射角，使每个输入模式覆盖较大的超表面孔径：

  $$
  \psi_i(\mathbf r)
  \approx P(\mathbf r)
  \exp\left[-i\frac{2\pi}{\lambda f}
  \boldsymbol\rho_i\cdot\mathbf r\right].
  $$

- 理想单色标量模型中，简单 $e^{i\phi(x,y)}$ 相位超表面可以完全实现薄凸透镜相位；二者的前向计算复杂度也基本相同。
- 凸透镜的主要收益是输入模式标准化、设计稳定和易于标定，而不是数量级地降低 FFT 计算量。
- 直接球面波求和不可行，但 Fresnel FFT 与凸透镜 Fourier 映射同为 $O(N\log N)$；角谱法通常只多一次 Fourier 变换。
- 固定透镜前端可折叠为解析 Fourier 输入算子；规则二维 LED 阵列的随机相干叠加通常仍需要一次二维 FFT。

## 推荐的第一层统一参数化

使用

$$
M_1(\mathbf r)
=\exp\left[i\phi_{\rm lens}(\mathbf r)
+i\Delta\phi_1(\mathbf r)\right].
$$

两种模式如下：

1. 固定模式：$\Delta\phi_1=0$ 且不求梯度。第一层在理想模型中等效于薄凸透镜，固定前端可折叠。
2. 可训练模式：$\Delta\phi_1$ 从零初始化并参与训练。第一层联合学习准直、模式整形和计算。

训练过程中不需要显式对相位做 $\bmod 2\pi$；只在最终制造导出时映射到 $[0,2\pi)$。

## 推荐训练流程

1. 先用单色、标量、理想相位模型验证系统。
2. 固定第一层为透镜相位，训练后续层并建立基线。
3. 解冻 $\Delta\phi_1$，使用较小学习率进行联合训练。
4. 大规模输入采用随机相位或正交编码直接估计 $\mathbf A\mathbf x$。
5. 多个随机实现先平均输出强度，再计算慢探测器对应的损失。
6. 逐步加入 LED 有限尺寸、角度、波长、偏振、相位量化和制造误差。
7. 最终用更多随机样本及抽样点源/相干模态精确传播验证。

## 必做消融实验

- 固定 $\phi_1=\phi_{\rm lens}$；
- $\phi_{\rm lens}$ 初始化后完全解冻第一层；
- 第一层带平滑、量化或制造约束；
- 固定透镜加一个额外可训练层。

同时比较任务误差、光效率、随机估计方差、收敛速度、宽谱/宽角鲁棒性和制造误差敏感度。

## 当前结论的适用范围

“简单相位超表面等效凸透镜”仅在理想单色、标量、局域纯相位模型中成立。真实超表面还可能具有

$$
t=t(\mathbf r;\lambda,\boldsymbol\theta_{\rm in},p),
$$

因此最终硬件决策仍需结合谱宽、入射角、偏振、效率和制造标定。

---

# 当前可落实性清单

以下分类采用已经确认的最终系统设定：

$$
N_{\rm in}=4096\times12=49\,152,
\qquad
N_{\rm out}=4096\times2=8192.
$$

其中 12 对应十二组不同的 LED 物理位置，输出的两组通道用于正负权重差分；超表面离线设计、静态制造。

## 一、当前确认能落实的点

### 1. 理论和前向模型

- 可以用互强度作为统一理论：

  $$
  \mathbf J_{\rm out}
  =\mathbf U_{\boldsymbol\theta}
  \mathbf J_{\rm in}
  \mathbf U_{\boldsymbol\theta}^{\dagger}.
  $$

- 对互不相干 LED 输入，可以严格得到强度线性变换：

  $$
  \mathbf y=\mathbf A_{\boldsymbol\theta}\mathbf x,
  \qquad
  A_{ji}=\left\|\mathbf D_j^{1/2}
  \mathbf U_{\boldsymbol\theta}\psi_i\right\|_2^2.
  $$

- 点源/LED 模式逐列传播可作为精确基准；随机相位平均可作为大规模无偏训练估计；部分相干 LED 可用 KL/相干模态分解。
- 角谱法、Fresnel 法、局域相位乘法、强度平均和探测器积分都可自动微分，因此反向传播仍是主方案。
- 无需显式存储四维互强度，也无需在每一步训练中构造完整 $8192\times49\,152$ 矩阵。
- 多波长、双偏振、有限 LED 发光面积和角度分布都能以“各互不相干模式强度加权和”的方式逐步加入。

### 2. 输入光路和第一层参数化

- 可将 LED 阵列放在透镜/metalens 前焦面，使不同 LED 映射为覆盖孔径的不同倾斜模式：

  $$
  \psi_i(\mathbf r)
  \approx P(\mathbf r)
  e^{-i2\pi\boldsymbol\rho_i\cdot\mathbf r/(\lambda f)}.
  $$

- 固定 LED—透镜传播可以折叠为解析输入模式或 Fourier 型固定算子，省去逐 LED 球面波求和。
- 第一层可以统一写为

  $$
  \phi_1=\phi_{\rm lens}+\Delta\phi_1.
  $$

  令 $\Delta\phi_1=0$ 并冻结，即为理想固定透镜模式；从零初始化并解冻，即为可训练的透镜残差模式。
- 可以先固定第一层训练后续层，再以较小学习率解冻；这允许在同一代码和器件概念中比较固定与可训练两条路线。
- 在理想单色标量模型里，纯相位 metalens 可以实现薄凸透镜相位；前向计算复杂度也同属 $O(Q\log Q)$ 数量级。

### 3. 非负和有符号矩阵编码

- $A_{ji}\ge0$ 与列和不超过 1 可以通过归一化、垃圾端口和电子增益处理。
- 任意非负目标 $\mathbf T$ 可按

  $$
  g=\max_i\sum_jT_{ji}
  $$

  缩放为列次随机矩阵，电子端恢复全局尺度。
- 任意实权重可用

  $$
  \mathbf W=\mathbf W^+-\mathbf W^-
  $$

  做正负双轨光学输出，再在电子端平衡做差。
- 输入输出维数不同不要求行和为 1；当前矩形映射不受“每行也必须归一化”的额外约束。

### 4. 输入模式可行性诊断

- 已经有独立项目 `incoherent_metasurface_modes/`，不会继续修改原光传播演示代码。
- 项目可以计算矩形/圆形孔径下的模式重叠、Gram 有效秩、目标列对必要可行性和两模式最优区分上界。
- 对规则 $49\,152$ 输入阵列，可在不形成 $49\,152^2$ Gram 矩阵的情况下做 $O(N)$ 有效秩估计。
- 当时已有 8 个单元测试通过；真实工程判断只需替换 pitch、发光面积、波长、焦距、孔径、NA 和角分布等参数。
- 若输入向量分量可重排，可以把目标矩阵相似列分配给物理上相近的 LED，从而降低非正交模式与任务需求的冲突。

### 5. 第一版工艺窗口

在真实 foundry 数据尚未确定时，以下参数可作为可仿真、可验证的原型起点，而不是最终规格：

- MicroLED pitch：$8\sim10\,\mu\mathrm m$；
- 波长：先做 $450\sim532\,\mathrm{nm}$ 单色或窄带；
- 焦距：约 $8\sim15\,\mathrm{mm}$；
- F/#：约 1.5–2.5；
- 有效孔径：约 $3\sim10\,\mathrm{mm}$。

理想圆孔分析中，$8\sim10\,\mu\mathrm m$ pitch、F/2、蓝绿光的输入有效秩接近 $49\,152$，足以作为继续做全传播和误差模型的候选方案。

### 6. 静态大规模超表面

- 当前器件是离线制造，不需要十亿级动态电极和控制线路。
- 十亿量级被动 meta-atom 总数在大面积、多层累计意义上并非物理数量级矛盾；已有更大 raw nanostructure count 的公开 metalens 先例。
- 可用有效相位像素训练，再通过 meta-atom library、局域电磁 surrogate 和多尺度流程导出制造结构，而不逐 raw cell 做端到端 FFT。

## 二、当前有风险但不能判定不可能的点

### 1. 能否实现目标矩阵族

- 完整矩阵有

  $$
  49\,152\times8192=402\,653\,184
  $$

  个元素；列归一化后仍约有 $402\,604\,032$ 个自由度。
- 总计约 $10^9$ raw 单元与 $2N_{\rm in}N_{\rm out}=805\,306\,368$ 的经验相位特征数量级接近，但这不是任意目标矩阵可实现的证明。
- 有限层自由传播与相位面形成的是高度结构化映射，参数数量足够仍可能因为 Jacobian 秩、局部极小值、空间带宽或模式截断而无法拟合目标。
- 实现某个任务优化后的结构化矩阵明显比“实现任意稠密随机矩阵”更可行；应通过小规模 scaling experiment 判断层数和有效参数需求。

### 2. 输入模式非正交

- LED 互不相干只消除交叉项，不保证单 LED 模式正交。
- 任意模式对满足

  $$
  \frac12\|\mathbf a_i-\mathbf a_k\|_1
  \le\sqrt{1-|\langle\psi_i,\psi_k\rangle|^2}.
  $$

- 理想参数下模式可以接近满秩，但有限发光面积、孔径、谱宽、像差、渐晕、封装和边缘场角可能显著降低有效秩。
- 目前的解析结果只采用理想均匀瞳和中心纯模式。没有真实 LED 与输入光路数据前，不能宣布非正交问题已经解决。

### 3. 输出隐藏模式和探测器结构

- 当前 $49\,152$ 输入压到 $8192$ 个电子输出；若希望无损保持输入正交，细光学输出空间至少有 $49\,152$ 维，即每个电子输出平均需容纳至少 6 个正交隐藏模式。
- 面积探测区域可以合并多空间模式，但具体能容纳多少独立模式取决于面积、NA、采样、像差和串扰。
- 抽象酉扩张证明列随机矩阵原则上可嵌入更大空间，却不保证当前自由空间多层器件能用有限孔径和有限层廉价实现。

### 4. 光效率和信噪比

- 列和小于 1、垃圾端口和正负双轨都可在数学上实现目标比例，但会损失光子。
- 正负两路数值很大而差值很小时，平衡探测受到散粒噪声、探测器不匹配、动态范围和漂移限制。
- 近 Lambertian LED 的几何收集比例可粗略只有 $\mathrm{NA}^2$；NA 0.25 时约 6.25%。收集效率可能比矩阵拟合误差更早成为瓶颈。
- 需要建立包含 LED 发光功率、收集、每层效率、垃圾端口、双轨差分、探测器 QE 和噪声的端到端光子预算。

### 5. 宽谱、宽角和偏振

- $20\sim30\,\mathrm{nm}$ 谱宽会移动模式重叠零点，也会让多层传播相位和 metalens 焦距随波长改变。
- 边缘 LED 入射角可能达到数度到十余度，真实 meta-atom 的相位与效率会随角度变化。
- 标量单偏振模型不能保证双偏振设备性能；最终需用 $t(\mathbf r;\lambda,\theta,p)$ 或矢量 surrogate 训练。
- 消色差、宽角、低 F/#、大发电孔径和高效率之间存在现实权衡。

### 6. $4\,\mu\mathrm m$ 及更小 pitch

- $4\,\mu\mathrm m$ pitch、蓝绿光、F/2 的理想中心模式结果较好，但有限发光区和工艺误差可能把它推入边缘区。
- $4\,\mu\mathrm m$ pitch 做红光/全彩时更依赖高 NA；$2.5\,\mu\mathrm m$ pitch、F/4 已明显有模式秩下降风险。
- 应先用真实发光面积和最长波长计算 Gram，再决定是否值得采用更小 pitch。

### 7. 十亿单元的制造和计算

- raw cell count 可达到不等于多层器件可高良率制造。层间纳米/微米级对准、翘曲、层距、封装、热漂移和大面积均匀性都可能限制性能。
- raw meta-atom 不等于独立相位自由度；邻近耦合、superpixel、有限几何 library 和空间带宽会降低有效参数数。
- 单场就需要约 8 GB complex64、单相位面约 4 GB float32；直接在 $10^9$ 网格上多层反向传播风险极高，必须采用有效像素、多尺度和分布式方案。

### 8. 第一层二合一 metalens

- $\phi_{\rm lens}+\Delta\phi_1$ 是最佳研究参数化，但真实二合一层可能为了计算功能偏离理想透镜，增加视场、色散、效率和制造敏感度。
- 独立凸透镜加额外可训练层更稳健但体积和层数更大；最终需靠四组消融与硬件误差模型选择，不能只比较理想训练损失。

## 三、在明确假设下不可能实现的点

这里的“不可能”指由线性代数、能量守恒或模式可区分性直接排除，而不是“以当前工艺很难”。

### 1. 无源系统不能产生净光功率增益

归一化无源输入下，不可能有目标探测通道列和超过 1：

$$
\sum_jA_{ji}>1.
$$

只能先全局缩放目标，再由电子端增益恢复数值尺度。电子增益不能恢复已经损失的光子 SNR。

### 2. 直接强度探测不能原生输出负功率

不可能由一个无源强度通道直接得到 $A_{ji}<0$。负权重必须使用正负双轨、偏置编码、相干检测或电子后处理。

### 3. 严格非相干源面上的局域纯相位第一层不能起作用

若

$$
\mathbf J_{\rm in}=\operatorname{diag}(\mathbf s),
\qquad
\mathbf M_1=\operatorname{diag}(e^{i\theta_1}),
$$

则恒有 $\mathbf M_1\mathbf J_{\rm in}\mathbf M_1^\dagger=\mathbf J_{\rm in}$。在这些严格假设下，第一层相位不可能影响任何后续统计输出，训练梯度也必为零。

### 4. 完全相同的两个输入模式不能产生不同矩阵列

若 $\psi_i=e^{i\alpha}\psi_k$，则任何共同线性光学系统和任何强度探测都给出

$$
\mathbf A_{:i}=\mathbf A_{:k}.
$$

更多相位层不能改变这一点。

### 5. 非正交模式不能被共同无损光学任意变成完全可区分输出

若 $|\langle\psi_i,\psi_k\rangle|>0$，则两输出概率列的总变差严格小于 1。因而在无附加来源标签、无有损后选择的假设下，不可能把它们确定性路由到两组完全不重叠的探测器。

### 6. 单模方阵 $A=|U|^2$ 不能覆盖全部非负或全部双随机矩阵

当输入输出是同维完整单模基、传播无损且无 pooling 时，$\mathbf A$ 必须是 unistochastic。多层相位只能改变 $U$，不能让 $|U|^2$ 覆盖非 unistochastic 双随机矩阵。探测器多模合并、损耗端口或其他扩张可以放宽这一特例，但不能声称原特例本身通用。

### 7. 8192 个严格单模输出不能无损承载 49152 个正交输入

若输出总模态空间真的只有 8192 维，而非 8192 个多模探测区域，则不可能通过无损线性光学保持 $49\,152$ 个输入模式的正交信息。要么提供至少 $49\,152$ 个细输出模式，要么允许损耗/降维/信息丢弃。

### 8. 相干强度输入不能在一般情况下按固定强度矩阵线性相加

若多个输入复振幅保持相干并同时点亮，平方律探测包含交叉项。除非相位受控并改用复振幅读出、各通道正交、时间平均去相干或采用其他专门编码，否则不可能对任意相干输入强度恒有固定的 $\mathbf y=\mathbf A\mathbf x$。

### 9. 十亿 raw 单元不能按当前朴素全量模型直接训练

若坚持每个 meta-atom 对应一个独立 FFT 网格点，并同时保存多层、多个随机样本的场、梯度和优化器状态，则常规单机 GPU 内存与计算量不可能承受。必须降低到有效光学像素、分块/分布式、多尺度或结构化参数化；这不是可选的小优化，而是必要的模型重构。

### 10. 仅凭层数或单元总数不能保证任意矩阵

只知道“层数可接受”和“单元约 $10^9$”时，不可能逻辑推出任意 $8192\times49\,152$ 非负矩阵都能实现。还必须满足输入可区分性、输出隐藏模式、能量、空间带宽、传播架构、参数 Jacobian 秩和制造响应等条件。

## 四、当前决策门槛

在进入完整多层训练前，应依次满足：

1. 用真实 LED 与前端参数确认输入 Gram 有效秩和最坏模式对；
2. 明确每个电子输出的细模式容量，验证至少约 6 个隐藏模式/输出是否有物理载体；
3. 完成端到端光子预算，确认双轨和垃圾端口后的 SNR；
4. 在缩小问题上测出层数、有效像素数与目标误差的 scaling；
5. 比较固定透镜、可训练 metalens 残差和独立透镜加额外训练层；
6. 再决定最终目标是“通用矩阵”、受结构约束矩阵，还是光电混合的任务端到端映射。
